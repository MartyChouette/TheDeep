using UnityEngine;

public class RoomPiece : MonoBehaviour
{
    // Connector definitions for each face.
    public bool _leftConnector;
    public bool _rightConnector;
    public bool _upConnector;
    public bool _downConnector;
    public bool _frontConnector;
    public bool _backConnector;

    // You can also include other properties, such as room type, etc.

    void Start()
    {
        // Initialization if needed
    }

    void Update()
    {
        // Per-frame logic if needed
    }
}